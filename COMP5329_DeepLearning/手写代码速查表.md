# COMP5329 手写代码速查表

> **来源（唯一标准）**：`5329/claude 复习/Materials2026/Tutorial/` 各周**主 notebook**  
> **收录**：Part B 各 Task + 该周无 Part B 时的核心实现块（§ 手推层）  
> **不收录**：Part C 文字题、`Self_Study`、Claude Code / Skills / MCP 章节  
> **用法**：按 Tutorial 周次序默写；每块对照 notebook 检查 shape。

### 星级（每个默写块）

| 星 | 含义 |
|----|------|
| `★★★` | 必默：Part B 核心 / 高频考点 |
| `★★` | 重要：常考或理解主干所必需 |
| `★` | 有余力：bullet、辅助函数、低优先级周 |

### 周标题颜色（整周优先级）

| 颜色 | 周 | 说明 |
|------|-----|------|
| <span style="color:#c0392b">■</span> 红 | Week3、Week4 | 必默·基础（手推 MLP + 正则） |
| <span style="color:#e67e22">■</span> 橙 | Week5、Week7、Week8 | 必默·主力（CNN / 序列） |
| <span style="color:#f39c12">■</span> 琥珀 | Week6、Week12 | 必默·次高（GCN / 生成） |
| <span style="color:#7f8c8d">■</span> 灰 | Week9、Week10、Week11 | 低优先级（考前有余力再默） |

---

## <span style="color:#c0392b">Week3 — Foundations `★★★`</span>

`Week3_Foundation.ipynb` · 手推 MLP（整周实现，无 Part B 标签）

### Weight Initialization `★★`

```python
# Xavier（Tanh / Sigmoid）
std = (2 / (fan_in + fan_out)) ** 0.5
W   = torch.randn(fan_in, fan_out) * std

# He（ReLU）
std = (2 / fan_in) ** 0.5
W   = torch.randn(fan_in, fan_out) * std

# 4D Conv：fan_in 含 kH*kW
fan_in  = C_in  * kH * kW
fan_out = C_out * kH * kW
```

### Activation Functions（Forward + Backward） `★★★`

```python
# Sigmoid
def forward(self, z):
    self.s = 1 / (1 + torch.exp(-z))
    return self.s
def backward(self, grad):
    return grad * self.s * (1 - self.s)

# Tanh
def forward(self, z):
    self.out = torch.tanh(z)
    return self.out
def backward(self, grad):
    return grad * (1 - self.out ** 2)

# ReLU
def forward(self, z):
    self.z = z
    return torch.clamp(z, min=0)
def backward(self, grad):
    return grad * (self.z > 0).float()
```

### HiddenLayer（Forward + Backward） `★★★`

```python
def forward(self, X):
    self.X = X
    z = X @ self.W + self.b
    return self.act.forward(z)

def backward(self, grad):
    dz = self.act.backward(grad)
    self.dW = self.X.T @ dz
    self.db = dz.sum(dim=0, keepdim=True)
    return dz @ self.W.T
```

### CrossEntropyLoss（Forward + Backward） `★★★`

```python
def forward(self, logits, labels):
    N = logits.shape[0]
    shifted = logits - logits.max(dim=1, keepdim=True).values
    log_sum = torch.log(torch.exp(shifted).sum(dim=1, keepdim=True))
    log_probs = shifted - log_sum
    self.probs, self.labels = torch.exp(log_probs), labels
    return -log_probs[torch.arange(N), labels].mean()

def backward(self):
    N = self.probs.shape[0]
    grad = self.probs.clone()
    grad[torch.arange(N), self.labels] -= 1.0
    return grad / N
```

### SGD / Momentum / Adam `★★`（Adam `★★★`）

```python
# Vanilla SGD
layer.W -= lr * layer.dW
layer.b -= lr * layer.db

# Momentum
layer.v_W = momentum * layer.v_W + layer.dW
layer.W  -= lr * layer.v_W

# Adam（单参数 θ；m,v,t 初始化 0）
t += 1
m = β1*m + (1-β1)*g;  v = β2*v + (1-β2)*g**2
m_hat = m / (1-β1**t);  v_hat = v / (1-β2**t)
θ -= lr * m_hat / (sqrt(v_hat) + ε)
```

---

## <span style="color:#c0392b">Week4 — Regularization `★★★`</span>

`Week4_Regularization.ipynb` · 手推正则 + `train()` 早停

### Inverted Dropout `★★★`

```python
keep = (torch.rand_like(out) > dropout_rate).float()
out  = out * keep / (1.0 - dropout_rate)   # train；test 不乘 mask
```

### Batch Normalization（Forward + Backward） `★★★`

```python
def forward(self, z):
    B = z.shape[0]
    mu, var = z.mean(0, keepdim=True), z.var(0, keepdim=True, unbiased=False)
    std = torch.sqrt(var + 1e-5)
    z_hat = (z - mu) / std
    self._cache = (z_hat, std, B)
    return self.gamma * z_hat + self.beta

def backward(self, grad):
    z_hat, std, B = self._cache
    dz_hat = grad * self.gamma
    dz = (1.0/(B*std)) * (B*dz_hat - dz_hat.sum(0,keepdim=True)
         - z_hat * (dz_hat*z_hat).sum(0,keepdim=True))
    return dz
```

### SGD + L2 Weight Decay `★★`

```python
layer.W -= lr * (dW + weight_decay * layer.W)
```

### Early Stopping（`train()` 核心逻辑） `★★`

```python
best_val, best_snap, no_imp = float("inf"), None, 0
for epoch in range(1, n_epochs + 1):
    # ... train one epoch → tr_losses.append(...)
    if X_val is not None:
        vl = criterion.forward(model.forward(X_val), y_val).item()
        if patience is not None:
            if vl < best_val - 1e-5:
                best_val, best_snap, no_imp = vl, model.snapshot(), 0
            else:
                no_imp += 1
                if no_imp >= patience:
                    model.restore(best_snap)
                    break
```

**记忆要点**：val 变好 → 存 snapshot、重置 patience；否则累计，满 patience 则 **restore best** 并停。

---

## <span style="color:#e67e22">Week5 — Convolutional Neural Networks `★★★`</span>

`Week5_CNN.ipynb` · §0–3 手推 + §5–7 `nn.Module`

### §0–1 · DataLoader + `transforms.Compose` `★★`

```python
from torchvision import datasets, transforms

transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((MEAN,), (STD,)),   # MNIST 版
])
# 增广示例（§1）
aug_transform = transforms.Compose([
    transforms.RandomRotation(15),
    transforms.RandomAffine(degrees=0, translate=(0.1, 0.1)),
    transforms.ToTensor(),
    transforms.Normalize((MEAN,), (STD,)),
])
train_loader = torch.utils.data.DataLoader(dataset, batch_size=64, shuffle=True)
```

### §2–3 · Conv 尺寸 + 手推 Conv / Pool `★★★`

```python
H_out = (H + 2 * padding - k) // stride + 1

def naive_conv2d(x, w, stride=1, padding=0):
    N, C_in, H, W = x.shape
    C_out, _, kH, kW = w.shape
    H_out = (H + 2*padding - kH) // stride + 1
    W_out = (W + 2*padding - kW) // stride + 1
    if padding > 0:
        x = F.pad(x, [padding]*4)
    out = torch.zeros(N, C_out, H_out, W_out)
    for i in range(H_out):
        for j in range(W_out):
            patch = x[:, :, i*stride:i*stride+kH, j*stride:j*stride+kW]
            out[:, :, i, j] = (patch.unsqueeze(1) * w.unsqueeze(0)).sum(dim=(2,3,4))
    return out

def max_pool2d(x, k=2, stride=2):
    N, C, H, W = x.shape
    H_out, W_out = (H-k)//stride+1, (W-k)//stride+1
    x_col = F.unfold(x, kernel_size=k, stride=stride)
    x_col = x_col.view(N, C, k*k, H_out*W_out)
    return x_col.max(dim=2).values.view(N, C, H_out, W_out)
```

### §5–6 · 架构要点（bullet，不默写整网 `Sequential`） `★`

| 架构 | 记什么 |
|------|--------|
| **AlexNet** | 更深 conv + ReLU + Dropout + LRN；ImageNet 224 RGB |
| **VGG-11** | 堆 **3×3**、same pad；小核叠感受野 |
| **ResNet** | **BasicBlock**：$F(x)+x$；解决 **degradation**（非过拟合） |

### §7 · `BasicBlock` `★★★` + `train_model` `★★`

> Tutorial 用自定义 `ConvLayer`；默写可用标准 `nn.Conv2d` 等价实现。

```python
class BasicBlock(nn.Module):
    def __init__(self, in_ch, out_ch, stride=1):
        super().__init__()
        self.conv1 = nn.Conv2d(in_ch, out_ch, 3, stride=stride, padding=1, bias=False)
        self.bn1   = nn.BatchNorm2d(out_ch)
        self.conv2 = nn.Conv2d(out_ch, out_ch, 3, stride=1, padding=1, bias=False)
        self.bn2   = nn.BatchNorm2d(out_ch)
        self.shortcut = nn.Identity()
        if stride != 1 or in_ch != out_ch:
            self.shortcut = nn.Sequential(
                nn.Conv2d(in_ch, out_ch, 1, stride=stride, bias=False),
                nn.BatchNorm2d(out_ch),
            )

    def forward(self, x):
        out = F.relu(self.bn1(self.conv1(x)))
        out = self.bn2(self.conv2(out))
        return F.relu(out + self.shortcut(x))   # F(x) + x
```

```python
def train_model(model, train_loader, test_loader, n_epochs=3, lr=1e-3, device="cpu"):
    model.to(device)
    opt, crit = torch.optim.Adam(model.parameters(), lr=lr), nn.CrossEntropyLoss()
    for epoch in range(n_epochs):
        model.train()
        for x, y in train_loader:
            x, y = x.to(device), y.to(device)
            opt.zero_grad()
            loss = crit(model(x), y)
            loss.backward()
            opt.step()
        model.eval()
        correct = total = 0
        with torch.no_grad():
            for x, y in test_loader:
                x, y = x.to(device), y.to(device)
                correct += (model(x).argmax(1) == y).sum().item()
                total += y.size(0)
        print(f"Epoch {epoch+1}  test_acc={correct/total:.4f}")
```

---

## <span style="color:#f39c12">Week6 — Graph Neural Networks `★★`</span>

`Week6_Graph_Neural_Networks.ipynb` · Part B

### `build_A_hat_norm` `★★★`

```python
def build_A_hat_norm(A):
    n = A.shape[0]
    A_hat = A + np.eye(n)
    D_inv_sqrt = np.diag(A_hat.sum(axis=1) ** (-0.5))
    return torch.tensor(D_inv_sqrt @ A_hat @ D_inv_sqrt, dtype=torch.float32)
```

### `GCNLayer` + 两层 `GCN` `★★★`

```python
class GCNLayer(nn.Module):
    def __init__(self, in_features, out_features, activation=True):
        super().__init__()
        self.W = nn.Parameter(torch.empty(in_features, out_features))
        self.activation = activation
        nn.init.xavier_uniform_(self.W)

    def forward(self, H, A_hat_norm):
        out = (A_hat_norm @ H) @ self.W
        return F.relu(out) if self.activation else out

class GCN(nn.Module):
    def __init__(self, in_f, hidden, num_classes):
        super().__init__()
        self.gcn1 = GCNLayer(in_f, hidden, True)
        self.gcn2 = GCNLayer(hidden, num_classes, False)

    def forward(self, H, A_hat_norm):
        return self.gcn2(self.gcn1(H, A_hat_norm), A_hat_norm)
```

**公式**：$H^{(l+1)} = \sigma(\tilde D^{-1/2}\tilde A\tilde D^{-1/2} H^{(l)} W^{(l)})$，$\tilde A = A + I$。

---

## <span style="color:#e67e22">Week7 — Sequence Modeling I `★★★`</span>

`Week7_Sequence_Modeling_Architectures_I.ipynb` · Part B Task B1 / B2

### Task B1 · `LSTMCell` `★★★`

```python
class LSTMCell(nn.Module):
    def __init__(self, in_features, hidden_size):
        super().__init__()
        self.gates = nn.Linear(in_features + hidden_size, 4 * hidden_size)

    def forward(self, x_t, state):
        h_prev, c_prev = state
        concat = torch.cat([h_prev, x_t], dim=1)
        i_pre, f_pre, g_pre, o_pre = self.gates(concat).chunk(4, dim=1)
        i_t = torch.sigmoid(i_pre)
        f_t = torch.sigmoid(f_pre)
        g_t = torch.tanh(g_pre)
        o_t = torch.sigmoid(o_pre)
        c_t = f_t * c_prev + i_t * g_t      # 加法更新
        h_t = o_t * torch.tanh(c_t)
        return h_t, c_t
```

### Task B2 · `MultiHeadSelfAttention` `★★★`

```python
class MultiHeadSelfAttention(nn.Module):
    def __init__(self, d_model, num_heads):
        super().__init__()
        assert d_model % num_heads == 0
        self.num_heads, self.d_k = num_heads, d_model // num_heads
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, d_model)
        self.W_v = nn.Linear(d_model, d_model)
        self.W_o = nn.Linear(d_model, d_model)

    def forward(self, x, mask=None):
        B, T, _ = x.shape
        H, d_k = self.num_heads, self.d_k
        Q = self.W_q(x).view(B, T, H, d_k).transpose(1, 2)
        K = self.W_k(x).view(B, T, H, d_k).transpose(1, 2)
        V = self.W_v(x).view(B, T, H, d_k).transpose(1, 2)
        scores = (Q @ K.transpose(-2, -1)) / math.sqrt(d_k)
        if mask is not None:
            scores = scores.masked_fill(mask == 0, float("-inf"))
        attn = F.softmax(scores, dim=-1)
        out = (attn @ V).transpose(1, 2).contiguous().view(B, T, -1)
        return self.W_o(out), attn
```

### `scaled_dot_product_attention`（辅助） `★★`

```python
scores = Q @ K.transpose(-2, -1) / math.sqrt(d_k)
if mask is not None:
    scores = scores.masked_fill(mask == 0, float("-inf"))
return torch.softmax(scores, dim=-1) @ V
```

---

## <span style="color:#e67e22">Week8 — Sequence Modeling II `★★★`</span>

`Week8_Sequence_Modeling_Architectures_II.ipynb` · Part B Task 1 / 2

### Task 1 · `ViTPatchEmbedding` `★★★`

```python
class ViTPatchEmbedding(nn.Module):
    def __init__(self, img_size, patch_size, in_chans, embed_dim):
        super().__init__()
        self.num_patches = (img_size // patch_size) ** 2
        self.unfold = nn.Unfold(kernel_size=patch_size, stride=patch_size)
        self.proj = nn.Linear(in_chans * patch_size**2, embed_dim)
        self.cls_token = nn.Parameter(torch.zeros(1, 1, embed_dim))
        self.pos_embed = nn.Parameter(torch.zeros(1, 1 + self.num_patches, embed_dim))

    def forward(self, x):
        B = x.shape[0]
        patches = self.unfold(x)
        tokens = self.proj(patches.transpose(1, 2))
        cls = self.cls_token.expand(B, -1, -1)
        tokens = torch.cat([cls, tokens], dim=1) + self.pos_embed
        return tokens
```

### Task 2 · SSM 卷积核 `★★`

```python
def build_ssm_kernel(A_bar, B_bar, C, L):
    K, A_pow_B = torch.zeros(L), B_bar.clone()
    for i in range(L):
        K[i] = (C @ A_pow_B).squeeze()
        A_pow_B = A_bar @ A_pow_B
    return K   # y = conv1d(u, K)
```

---

## <span style="color:#7f8c8d">Week9 — MultiModal Foundation Models `★`</span>

`Week9 - MultiModal_Foundation_Models.ipynb` · Part B · 低优先级

### Task B1 · `CrossAttention` `★★`

```python
class CrossAttention(nn.Module):
    def __init__(self, d_model, num_heads):
        super().__init__()
        self.num_heads, self.d_k = num_heads, d_model // num_heads
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, d_model)
        self.W_v = nn.Linear(d_model, d_model)
        self.W_o = nn.Linear(d_model, d_model)

    def forward(self, x_query, x_memory, memory_mask=None):
        B, T_q, _ = x_query.shape
        T_m = x_memory.size(1)
        H, d_k = self.num_heads, self.d_k
        Q = self.W_q(x_query).view(B, T_q, H, d_k).transpose(1, 2)
        K = self.W_k(x_memory).view(B, T_m, H, d_k).transpose(1, 2)
        V = self.W_v(x_memory).view(B, T_m, H, d_k).transpose(1, 2)
        scores = (Q @ K.transpose(-2, -1)) / math.sqrt(d_k)
        if memory_mask is not None:
            scores = scores.masked_fill(memory_mask[:, None, None, :] == 0, float("-inf"))
        attn = F.softmax(scores, dim=-1)
        context = (attn @ V).transpose(1, 2).contiguous().view(B, T_q, -1)
        return self.W_o(context), attn
```

### Task B2 · `KVCache` + 增量 K/V `★★`

```python
class KVCache:
    def __init__(self):
        self.K = self.V = None

    def update(self, new_k, new_v):    # (B, H, 1, d_k)
        if self.K is None:
            self.K, self.V = new_k, new_v
        else:
            self.K = torch.cat([self.K, new_k], dim=2)
            self.V = torch.cat([self.V, new_v], dim=2)
        return self.K, self.V
```

**记忆要点**：decode 每步只算新 token 的 $Q,K,V$；$K,V$ **append** 到 cache；attention 对**全长** cache 做。

---

## <span style="color:#7f8c8d">Week10 — Deep Reinforcement Learning `★`</span>

`Week10_Deep_Reinforcement_Learning.ipynb` · Part B · 低优先级

### Task B1–B3 · DQN `★★`

```python
class QNetwork(nn.Module):
    def __init__(self, state_dim, n_actions, hidden=32):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(state_dim, hidden), nn.ReLU(),
            nn.Linear(hidden, hidden), nn.ReLU(),
            nn.Linear(hidden, n_actions),
        )
    def forward(self, x):
        return self.net(x)

class ReplayBuffer:
    def __init__(self, capacity=10000):
        self.buffer = deque(maxlen=capacity)
    def push(self, s, a, r, s_next, done):
        self.buffer.append((s, a, r, s_next, done))
    def sample(self, batch_size):
        batch = random.sample(self.buffer, batch_size)
        s, a, r, sn, d = zip(*batch)
        return (torch.as_tensor(np.array(s), dtype=torch.float32),
                torch.as_tensor(a, dtype=torch.long),
                torch.as_tensor(r, dtype=torch.float32),
                torch.as_tensor(np.array(sn), dtype=torch.float32),
                torch.as_tensor(d, dtype=torch.float32))

def dqn_loss(q_net, target_net, batch, gamma=0.9):
    s, a, r, s_next, done = batch
    q_sa = q_net(s).gather(1, a[:, None]).squeeze(1)
    with torch.no_grad():
        target = r + gamma * target_net(s_next).max(1).values * (1 - done)
    return F.mse_loss(q_sa, target)
```

### Task B4 · `ppo_loss` `★`

```python
def ppo_loss(log_probs_new, log_probs_old, advantages, clip_eps=0.2):
    ratio = torch.exp(log_probs_new - log_probs_old)
    unclipped = ratio * advantages
    clipped   = torch.clamp(ratio, 1 - clip_eps, 1 + clip_eps) * advantages
    return -torch.min(unclipped, clipped).mean()
```

---

## <span style="color:#7f8c8d">Week11 — Self-Supervised Learning `★`</span>

`Week11_Self_Supervised_Representation_Learning.ipynb` · Part B · 低优先级

### SimCLR · `nt_xent_loss` `★★`

```python
def nt_xent_loss(z1, z2, temperature=0.5):
    B = z1.shape[0]
    z = torch.cat([z1, z2], dim=0)                    # (2B, D)
    sim = (z @ z.T) / temperature
    mask = ~torch.eye(2 * B, dtype=torch.bool, device=z.device)
    sim = sim.masked_select(mask).view(2 * B, 2 * B - 1)
    labels = torch.cat([torch.arange(B, 2*B), torch.arange(B)], device=z.device)
    return F.cross_entropy(sim, labels)
```

### MAE · `random_masking` `★★`

```python
def random_masking(patches, mask_ratio):
    B, N, D = patches.shape
    n_keep = int(N * (1.0 - mask_ratio))
    noise = torch.rand(B, N, device=patches.device)
    ids_shuffle = noise.argsort(dim=1)
    ids_restore = ids_shuffle.argsort(dim=1)
    ids_keep = ids_shuffle[:, :n_keep]
    x_visible = torch.gather(patches, 1, ids_keep.unsqueeze(-1).expand(-1, -1, D))
    mask = torch.ones(B, N, device=patches.device)
    mask[:, :n_keep] = 0
    mask = torch.gather(mask, 1, ids_restore)
    return x_visible, mask, ids_restore
```

**记忆要点**：loss 只在 **被 mask 的 patch** 上算（分母用 `mask.sum()`，不是 `numel()`）。

---

## <span style="color:#f39c12">Week12 — Deep Generative Models `★★★`</span>

`Week12_Deep_Generative_Models.ipynb` · Part B Task 1 (GAN) + Task 2 (DDPM)

### Task B2 · `gan_step`（非饱和） `★★★`

```python
def gan_step(G, D, real_batch, g_opt, d_opt, latent_dim):
    B = real_batch.size(0)
    z = torch.randn(B, latent_dim, device=real_batch.device)
    fake = G(z).detach()
    d_loss = (F.binary_cross_entropy_with_logits(D(real_batch), torch.ones(B, 1, device=real_batch.device))
            + F.binary_cross_entropy_with_logits(D(fake), torch.zeros(B, 1, device=real_batch.device)))
    d_opt.zero_grad(); d_loss.backward(); d_opt.step()

    z = torch.randn(B, latent_dim, device=real_batch.device)
    g_loss = F.binary_cross_entropy_with_logits(D(G(z)), torch.ones(B, 1, device=real_batch.device))
    g_opt.zero_grad(); g_loss.backward(); g_opt.step()
    return d_loss.item(), g_loss.item()
```

**记忆要点**：训 D 时 `fake.detach()`；G 用 **−log D(G(z))**（BCE target=1）。

### Task B3–B5 · DDPM `★★★`

```python
def q_sample(x0, t, noise, sqrt_ab, sqrt_1m_ab):
  # x_t = sqrt(ᾱ_t) x0 + sqrt(1-ᾱ_t) ε
    sa  = sqrt_ab[t].view(-1, *([1] * (x0.dim() - 1)))
    s1a = sqrt_1m_ab[t].view(-1, *([1] * (x0.dim() - 1)))
    return sa * x0 + s1a * noise, noise

def ddpm_loss(eps_model, x0, sqrt_ab, sqrt_1m_ab, T):
    t = torch.randint(0, T, (x0.shape[0],), device=x0.device)
    noise = torch.randn_like(x0)
    x_t, eps = q_sample(x0, t, noise, sqrt_ab, sqrt_1m_ab)
    return F.mse_loss(eps_model(x_t, t.float()), eps)

@torch.no_grad()
def ddpm_reverse_step(eps_model, x_t, t, schedule):
    betas, alphas, alpha_bars = schedule["betas"], schedule["alphas"], schedule["alpha_bars"]
    B = x_t.size(0)
    eps_pred = eps_model(x_t, torch.full((B,), t, device=x_t.device, dtype=torch.float))
    beta_t, alpha_t, ab_t = betas[t], alphas[t], alpha_bars[t]
    mu = (x_t - beta_t / (1 - ab_t).sqrt() * eps_pred) / alpha_t.sqrt()
    if t > 0:
        return mu + betas[t].sqrt() * torch.randn_like(x_t)
    return mu
```

---

## 附录 · 形状备忘 `★★`

| 操作 | 输入 shape | 输出 shape |
|------|-----------|-----------|
| Linear `X@W` | `(N, F_in)` | `(N, F_out)` |
| Conv unfold | `(N, C, H, W)` | `(N, C·kH·kW, L)` |
| GCN `A@H` | `(N,N)·(N,F)` | `(N, F)` |
| BN | `(N, F)` | `(N, F)`（mean/var 沿 batch） |
| MHA | `x:(B,T,D)` | `attn:(B,H,T,T)` |
