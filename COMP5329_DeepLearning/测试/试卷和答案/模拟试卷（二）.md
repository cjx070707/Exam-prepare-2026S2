# COMP5329 Deep Learning — 模拟试卷（二）

> **考试格式（官方）**：Final exam for COMP4329 / COMP5329  
> *The exam consists of **5 choice questions** and **8 short-answer questions**, including **coding tasks** and **basic calculations**.*
>
> **出题参考**：① `以往真题试卷.md`（购买真题）② `老师给的模拟题.pdf`（老师样卷）。  
> **本套侧重**：补第一套没重点考的考点——**权重初始化 / GNN / 自监督 / ViT / 连续深度网络 / 强化学习对齐**。  
> **风格**：选择题“无破绽”；简答为新情境（调试/决策/诊断/计算）；**coding 题仿真题 Q7**（给骨架、补全函数，三小问全是写码）+ 2 道 novel-context 压轴。
>
> **用法**：先不看答案做完，再展开 `> [!success]- Answer` 对照「📌考点 / 大白话 / 得分点」。

---

## Multiple Choice Questions（5 × 选择题）

### 1. 激活函数与初始化（选 incorrect）

Which of the following statements is **incorrect**?

- [ ] A. He (Kaiming) initialization uses a larger weight variance than Xavier for ReLU networks, partly to compensate for ReLU zeroing out roughly half of the activations.
- [ ] B. Initializing all weights in a layer to the same constant prevents the neurons from learning distinct features, because they receive identical gradients.
- [ ] C. Leaky ReLU keeps a small nonzero slope for negative inputs, which helps avoid neurons that stop updating entirely.
- [ ] D. Tanh is preferred over ReLU in very deep networks because, being zero-centered, it does not suffer from vanishing gradients.

> [!success]- Answer
>
> **📌 考 W2 激活函数 + 初始化。** ✅「第一组」Part 2 + Weight Init。
>
> **大白话：** A/B/C 都对。D 说反了——Tanh 虽零中心，但两端**仍会饱和**、深网络照样梯度消失；正是 ReLU（正区间不饱和）才缓解了它。
>
> **得分点：**
> - **选 D（incorrect）** — Tanh 仍饱和、深层仍 vanishing；ReLU 才是缓解梯度消失的主因，不是 Tanh。
> - A（He 方差更大补偿 ReLU 置零）、B（同值初始化→对称无法学）、C（Leaky ReLU 防 dead neuron）均正确。

---

### 2. 图神经网络（选 incorrect）

Which of the following statements about GCNs is **incorrect**?

- [ ] A. Adding a self-loop ($\tilde A = A + I$) lets a node's own features participate in the aggregation, which plain $A$ omits.
- [ ] B. Symmetric normalization $\tilde D^{-1/2}\tilde A\tilde D^{-1/2}$ prevents high-degree nodes from dominating the aggregated representation.
- [ ] C. Stacking many GCN layers improves node classification monotonically, because each extra layer strictly adds discriminative information.
- [ ] D. A GCN is permutation-equivariant: relabeling the nodes permutes the outputs correspondingly rather than changing them.

> [!success]- Answer
>
> **📌 考 W5 GCN / over-smoothing。** ✅「第二组」Part 4。
>
> **大白话：** A/B/D 都对。C 错——层数太多会 **over-smoothing**：反复邻域平均让所有节点表示趋同，反而**降**性能，不是单调变好。
>
> **得分点：**
> - **选 C（incorrect）** — 深层 GCN 反复平均 → over-smoothing，节点表示收敛趋同 → 性能下降，需 residual/JK 才能堆深。
> - A（自环纳入自身）、B（对称归一化抑制高度数主导）、D（permutation equivariance）均正确。

---

### 3. ViT vs CNN（选 correct）

Which statement about Vision Transformers (ViT) versus CNNs is **correct**?

- [ ] A. CNNs build in locality and translation-equivariance priors, so they tend to outperform ViTs when training data is limited.
- [ ] B. ViTs contain a built-in locality prior, which is why they need far less data than CNNs to generalize.
- [ ] C. A ViT does not need positional information, because self-attention already encodes the spatial order of patches.
- [ ] D. CNNs and ViTs have the same per-layer asymptotic cost, since both ultimately apply matrix multiplications.

> [!success]- Answer
>
> **📌 考 W4/W8 inductive bias。** ✅「第二组」Inductive Bias +「第三组」ViT。
>
> **大白话：** A 对——CNN 自带局部性 + 平移等变先验，小数据更省、更易学；ViT 无此先验、需大数据。
>
> **得分点：**
> - **选 A（correct）**
> - **B 错**：ViT **没有**内建 locality 先验（正相反）。**C 错**：self-attention 置换不变，必须加位置编码。**D 错**：CNN $\approx O(HW\,k^2C)$，ViT $\approx O(T^2 d)$，量级不同。

---

### 4. 自监督学习（选 incorrect）

Which of the following statements about self-supervised representation learning is **incorrect**?

- [ ] A. In contrastive learning, the temperature $\tau$ controls how much weight hard negatives receive; a smaller $\tau$ sharpens focus on the hardest negatives.
- [ ] B. SimCLR relies on large batches to supply many negatives, whereas MoCo uses a momentum-updated queue to decouple the negative count from batch size.
- [ ] C. BYOL avoids representational collapse without negatives by using an asymmetric design (stop-gradient on the target plus a predictor on the online branch).
- [ ] D. MAE works well because it masks only a small fraction (around 15%) of patches, keeping the reconstruction task easy enough for stable training.

> [!success]- Answer
>
> **📌 考 W11 对比学习 / BYOL / MAE。** ✅「第四组」Week 11。
>
> **大白话：** A/B/C 都对。D 错——MAE 的关键恰恰是**高掩码率（约 75%）**，把任务变难、逼 encoder 学全局语义；15% 是 BERT MLM 的比例。
>
> **得分点：**
> - **选 D（incorrect）** — MAE 用 ~75% 高掩码率（任务难→学全局语义），不是 15%（那是 BERT）。
> - A（小 $\tau$ 聚焦 hard negatives）、B（SimCLR 大 batch vs MoCo 动量队列）、C（BYOL 不对称防 collapse）均正确。

---

### 5. 对齐算法（选 incorrect）

Which of the following statements about RLHF and its alternatives is **incorrect**?

- [ ] A. RLHF with PPO typically keeps four models in memory (policy, frozen reference, reward, and value/critic), which motivates lighter alternatives.
- [ ] B. The KL penalty toward the reference policy discourages the policy from drifting into reward-hacking regions far from the SFT model.
- [ ] C. DPO removes the separately trained reward model by expressing the preference objective directly in terms of the policy and reference log-probabilities.
- [ ] D. GRPO estimates the advantage with a separately trained value network, which is what lets it discard the reference model entirely.

> [!success]- Answer
>
> **📌 考 W10 RLHF / DPO / GRPO。** ✅「第四组」Week 10。
>
> **大白话：** A/B/C 都对。D 说反了——GRPO 恰恰是**用组内标准化替代 value/critic**（省的是 critic，不是 reference），而且它**保留** reference 做 KL。
>
> **得分点：**
> - **选 D（incorrect）** — GRPO 用组内 $\hat A_i=(r_i-\text{mean})/\text{std}$ 替代 **critic**（省 value 网络），仍保留 reference；不是“用 value 网络去掉 reference”。
> - A（PPO 4 模型）、B（KL 防 reward hacking）、C（DPO 隐式 reward）均正确。

---

## Short-Answer / Coding Questions（8 × 简答，含 coding + 计算）

### Q1 — 权重初始化（场景诊断）

A 20-layer ReLU MLP barely trains: the loss hardly moves and you observe that activations in deep layers shrink toward zero as you go deeper.

- **(a)** Explain why a poor initialization can cause activations (and gradients) to shrink or explode across many layers. **(4 Marks)**
- **(b)** Which initialization scheme is appropriate for ReLU, and why does it use a different variance than the one designed for tanh/sigmoid? **(4 Marks)**
- **(c)** What goes wrong if every weight in a layer is initialized to the same constant? **(2 Marks)**

> [!success]- Answer
>
> **📌 考 W2 Weight Initialization + 梯度消失/爆炸。** ✅「第一组」Weight Init / Sigmoid。
>
> **大白话：** 初始化方差不对，信号每过一层就被放大或缩小，多层连乘后爆炸或消失。ReLU 会把约一半激活置零，需要更大的初始方差（He）来补偿；tanh 用 Xavier。所有权重相同会导致神经元完全对称、学不出不同特征。
>
> **得分点：**
>
> **(a)** 前向/反向每层乘一次权重矩阵；若每层增益 $\neq1$，多层**连乘**使激活/梯度指数级**放大（爆炸）或衰减（消失）**。合理初始化让逐层方差大致守恒。
>
> **(b)** ReLU 用 **He/Kaiming**（方差 $\propto 2/n_\text{in}$）；因为 ReLU 把约一半输出置零、削掉一半方差，需**更大**初始方差补偿。**Xavier**（$\propto1/n_\text{in}$ 或 $2/(n_\text{in}+n_\text{out})$）为 tanh/sigmoid 设计，保持前向方差一致。
>
> **(c)** 同一层所有权重相同 → 所有神经元收到**相同梯度、做相同更新**（对称无法打破）→ 等价于一个神经元，学不出多样特征。

---

### Q2 — Dropout 与 BN 的冲突（调试）

A network trains fine, but at test time accuracy drops sharply. The architecture stacks, in order, `Dropout → BatchNorm` inside each block.

- **(a)** BN's training and inference behaviors differ. Briefly state how. **(3 Marks)**
- **(b)** Explain why placing Dropout right **before** BN can cause a train/test mismatch in BN's statistics. **(5 Marks)**
- **(c)** Suggest one concrete fix. **(2 Marks)**

> [!success]- Answer
>
> **📌 考 W3 BN / Dropout 行为差异。** ✅「第一组」BN + Dropout（推理推得）。
>
> **大白话：** BN 训练用 batch 统计、测试用 running 统计。Dropout 训练时随机置零会改变送入 BN 的激活**方差**；BN 把这个“带 dropout 噪声”的统计存进 running stats，但测试时 dropout 关闭、方差变了 → BN 统计对不上 → 测试掉点。
>
> **得分点：**
>
> **(a)** 训练：用当前 mini-batch 的 $\mu_B,\sigma_B^2$ 归一化并更新 running 统计；推理：用固定的 **running mean/var**（不再依赖当前 batch）。
>
> **(b)** Dropout 在训练时随机置零 + 缩放，改变了进入 BN 的激活**分布/方差**；BN 据此累积的 running 统计带有“dropout 开启”的方差。测试时 dropout **关闭**，真实方差变化 → BN 用错的 running 统计归一化 → train/test **variance shift** → 掉点。
>
> **(c)** 任选：把 **Dropout 移到 BN 之后**（或所有 BN 之后）/ 该块**只用其一** / 用对 dropout 不敏感的归一化。

---

### Q3 — Bottleneck 参数预算（情境里藏计算）

A residual block must transform a $256$-channel feature map back to $256$ channels with a $3\times3$ convolution at its core. A direct $3\times3$ conv ($256\to256$) is too expensive.

- **(a)** Compute the parameter count of the direct $3\times3$ conv ($256\to256$, ignore bias). **(3 Marks)**
- **(b)** Design a $1\times1 \to 3\times3 \to 1\times1$ bottleneck (reduce to 64 channels in the middle) and compute its total parameters. **(5 Marks)**
- **(c)** Besides parameters, what other benefit does the $1\times1$ reduction give, and what is a potential downside of an aggressive bottleneck? **(3 Marks)**

> [!success]- Answer
>
> **📌 考 W4 1×1 conv / bottleneck（ResNet）。** ✅「第二组」1×1 Conv + ResNet。
>
> **大白话：** 直接 3×3 在 256→256 很贵；先 1×1 降到 64、再 3×3、再 1×1 升回 256，参数骤降约 8×。1×1 还能跨通道混合 + 加非线性；但压太狠会丢信息。
>
> **得分点：**
>
> **(a)** 直接 $3\times3$：$256\times256\times3\times3=\mathbf{589{,}824}$。
>
> **(b)** Bottleneck：
> - $1\times1$（256→64）：$256\times64=16{,}384$
> - $3\times3$（64→64）：$64\times64\times9=36{,}864$
> - $1\times1$（64→256）：$64\times256=16{,}384$
> - **合计 $=69{,}632$**，约为直接卷积的 **1/8.5**。
>
> **(c)** 1×1 还做**跨通道线性组合 + 额外非线性（接 ReLU）**；下行风险：中间通道压得过狭 → **信息瓶颈**、表达能力受限。

---

### Q4 — 深层 GCN 退化（场景诊断）

You stack more GCN layers hoping to capture larger neighborhoods, but node-classification accuracy **drops** as depth increases, and node embeddings become hard to tell apart.

- **(a)** Name this phenomenon and explain its mechanism in terms of repeated neighborhood aggregation. **(4 Marks)**
- **(b)** Why does it specifically worsen with depth? **(3 Marks)**
- **(c)** Propose two architectural remedies that let GCNs go deeper. **(3 Marks)**

> [!success]- Answer
>
> **📌 考 W5 over-smoothing。** ✅「第二组」Spatial GCN（limitations）。
>
> **大白话：** 这是 **over-smoothing**——每层都做归一化邻域平均，层数多了所有节点表示被反复平均到趋同，类别边界消失，所以越深越差。
>
> **得分点：**
>
> **(a)** **Over-smoothing**：GCN 每层 $\approx$ 归一化邻域平均（$\tilde D^{-1/2}\tilde A\tilde D^{-1/2}H$），反复平均使相连节点表示趋同 → 失去判别性。
>
> **(b)** 深度 = 平均次数；层数越多，邻域感受野越大、平均轮数越多 → 全图节点表示收敛到相似值（极限趋于由度决定的稳态）→ 越深越糊。
>
> **(c)** 任选两：**residual / skip connection**、**Jumping Knowledge（聚合各层表示）**、限制层数、**PairNorm / 归一化技巧**、加 gating。

---

### Q5 — GCN + Contrastive Loss（coding · 仿真题 Q7）

Complete the three functions below for a node-classification pipeline that also uses contrastive pretraining.

1. Build the **symmetrically normalized adjacency** $\tilde D^{-1/2}\tilde A\tilde D^{-1/2}$ where $\tilde A = A + I$.

```python
def build_A_hat_norm(A):
    """A: (n, n) numpy adjacency. Return torch.FloatTensor."""
    # Your code here
    return A_hat_norm
```

2. Complete the **GCN layer** forward pass: $H' = \sigma(\tilde A H W)$.

```python
class GCNLayer(nn.Module):
    def __init__(self, in_features, out_features):
        super().__init__()
        self.W = nn.Parameter(torch.empty(in_features, out_features))
        nn.init.xavier_uniform_(self.W)

    def forward(self, H, A_hat_norm):
        # Your code here
        return F.relu(out)
```

3. Complete **InfoNCE / NT-Xent** for two view batches `z1, z2` of shape `(N, d)` (positive pair: `z1[i]` ↔ `z2[i]`).

```python
def info_nce(z1, z2, tau=0.5):
    """Return scalar contrastive loss."""
    N = z1.shape[0]
    # Your code here: normalize → sim/τ → mask self → cross_entropy
    return loss
```

> [!success]- Answer
>
> **📌 考 W6 GCN + W11 InfoNCE。** ✅「第二组」GCN +「第四组」对比学习；速查表 Week6、Week11。
>
> **大白话：** 三问全是补全：归一化邻接矩阵、GCN 一层前向、SimCLR 损失。和真题 Q7 一样只写代码，不附论述小问。
>
> **得分点：**
>
> **(1) build_A_hat_norm**
> ```python
> import numpy as np
> import torch
>
> def build_A_hat_norm(A):
>     n = A.shape[0]
>     A_hat = A + np.eye(n)
>     D_inv_sqrt = np.diag(A_hat.sum(axis=1) ** (-0.5))
>     return torch.tensor(D_inv_sqrt @ A_hat @ D_inv_sqrt, dtype=torch.float32)
> ```
>
> **(2) GCNLayer.forward**
> ```python
> def forward(self, H, A_hat_norm):
>     out = (A_hat_norm @ H) @ self.W
>     return F.relu(out)
> ```
>
> **(3) info_nce**
> ```python
> import torch.nn.functional as F
>
> def info_nce(z1, z2, tau=0.5):
>     N = z1.shape[0]
>     z = F.normalize(torch.cat([z1, z2], dim=0), dim=1)
>     sim = z @ z.t() / tau
>     sim.fill_diagonal_(float("-inf"))
>     targets = torch.cat([torch.arange(N) + N, torch.arange(N)]).to(z.device)
>     return F.cross_entropy(sim, targets)
> ```
>
> **与速查表**：Week6 `build_A_hat_norm` + `GCNLayer` `★★★`；Week11 `nt_xent_loss` `★★`。

---

### Q6 — 位置编码与外推（工程决策）

A Transformer trained on sequences up to length 512 must now handle inputs of length 2000 at inference.

- **(a)** Why does a standard Transformer need positional encoding at all? **(3 Marks)**
- **(b)** Compare **sinusoidal** vs **learned** positional encodings for this length-extrapolation scenario, and say which you would choose and why. **(5 Marks)**
- **(c)** Independent of position encoding, what is the main cost concern when the sequence grows to length 2000? **(3 Marks)**

> [!success]- Answer
>
> **📌 考 W8 positional encoding + 复杂度。** ✅「第三组」Transformer 架构。
>
> **大白话：** self-attention 置换不变，必须注入位置。Sinusoidal 是固定函数、能外推到训练没见过的更长位置；learned 是查表参数，超出训练长度就没有对应向量、无法外推。要处理 2000>512，选 sinusoidal（或可外推的相对位置编码）。另外长度大，$O(T^2)$ 成本是主瓶颈。
>
> **得分点：**
>
> **(a)** self-attention **permutation-invariant**，无内建顺序 → 需位置编码注入 token 顺序。
>
> **(b)** **Sinusoidal**：固定正弦函数、无需训练，**能外推**到更长位置（512→2000 仍有定义）。**Learned**：可训练查表，更灵活但**不能外推**（>512 无对应 embedding）。本场景选 **sinusoidal**（或 relative/RoPE 类可外推方案）。
>
> **(c)** self-attention 的 $O(T^2\cdot d)$ 时间/内存——$T=2000$ 时平方级开销（KV/注意力矩阵）成为主要瓶颈。

---

### Q7 — Neural ODE（连续深度网络）（novel-context 压轴 · 论述）

> *研究扩展型 novel-context 题：Neural ODE 不是讲义专章，背景已给；要求用你学过的 **ResNet 残差块 / 反向传播显存 / 深度-计算权衡** 等核心知识推理作答。*

**背景：** A residual block computes $h_{l+1}=h_l+f(h_l,\theta_l)$. **Neural ODEs** notice this looks like one step of solving an ordinary differential equation $\frac{dh}{dt}=f(h(t),\theta)$. Instead of stacking a fixed number of discrete layers, a Neural ODE defines a **continuous-depth** transformation and hands $f$ to a numerical **ODE solver** that integrates from $t=0$ to $t=1$; the "number of layers" becomes the number of solver steps, which the solver can choose **adaptively**. Gradients can be obtained by the **adjoint method**, which solves a second ODE backward instead of storing every intermediate activation.

- **(a)** Show precisely how one ResNet block corresponds to a single step of **Euler integration** of the ODE, and state the implied **step size**. **(4 Marks)**
- **(b)** A standard $N$-layer ResNet stores all intermediate activations for backprop, costing $O(N)$ memory in depth. Explain why the continuous (adjoint) formulation can cut this memory cost, and what trade-off it introduces. **(5 Marks)**
- **(c)** Being able to choose the number of solver steps **adaptively** trades compute for accuracy. Drawing on the depth/compute trade-offs you know from CNNs, give one advantage of spending **more** steps at inference and one risk of spending **too few**. **(5 Marks)**

> [!success]- Answer
>
> **📌 考 novel-context（Neural ODE ↔ ResNet）。** ❌ 笔记无专章 → 用「第二组」ResNet 残差 +「第一组」反向传播/显存推理。
>
> **大白话：** ResNet 的 $h_{l+1}=h_l+f(h_l)$ 正好是 ODE 的一步 Euler（步长=1）。把离散层换成连续积分后，反向用 adjoint 重解一个 ODE 拿梯度，就不用存每层激活 → 显存从 $O(N)$ 降到约 $O(1)$，代价是要多算（重积分 + 数值误差）。步数可调：多步更精但更慢，太少会积分误差大、结果跑偏。
>
> **得分点：**
>
> **(a)** Euler 一步：$h(t+\Delta t)=h(t)+\Delta t\cdot f(h(t),\theta)$。与 ResNet $h_{l+1}=h_l+f(h_l,\theta_l)$ 逐项对应 → **步长 $\Delta t=1$**，残差分支 $f$ 即导数函数。所以**一个残差块 = 步长 1 的一次 Euler 积分**。
>
> **(b)** Adjoint：反向时**不存所有中间激活**，而是从输出沿时间**反解一个伴随 ODE** 重建状态/梯度 → 深度方向显存约 $O(1)$（与“层数/步数”无关）。**权衡**：需**额外计算**（反向重新积分）、依赖数值重建的**精度**（误差累积/可逆性问题）、求解器本身有开销与不确定步数。
>
> **(c)** **多步优点**：积分更细 → 数值更精，可在**推理时**用更多步换更高精度而**无需重训**（连续深度 = 可调“深度”，对难/刚性动态多花步数）。**太少风险**：积分误差大 → 输出偏离真解、精度骤降、甚至不稳定（类比 CNN 砍层降容量伤精度，这里是“步数不足→离散化误差”）。

---

### Q8 — 设计奖励信号对齐 LLM（novel-context 压轴 · 论述）

> *研究扩展型 novel-context 题：把语言生成当作 RL 问题，背景已给；要求结合课上 **RL / 策略梯度 / 监督学习** 知识推理。*

**背景：** You want a chatbot to produce more helpful answers. Humans find it hard to assign an absolute score to a response, but can reliably say "response A is better than response B". You decide to align the model with this preference feedback.

- **(a)** Why can't you simply fine-tune with standard supervised learning on a numeric "quality" label? Frame the language-generation task as an RL problem (what are state, action, reward?). **(4 Marks)**
- **(b)** Outline a pipeline that turns **pairwise preferences** into a usable reward and then optimizes the policy. Name the role of each stage. **(5 Marks)**
- **(c)** When optimizing against a learned reward, the model may "game" it (reward hacking). What mechanism prevents the policy from drifting too far, and why does it work? **(5 Marks)**

> [!success]- Answer
>
> **📌 考 novel-context（W10 RLHF）。** ✅「第四组」Week 10（RLHF / Bradley-Terry / KL）。
>
> **大白话：** 人给不出精确分数、只能给相对偏好，所以不能直接监督回归。把生成当 RL：state=prompt+已生成 token，action=下一个 token，reward=人类偏好分。流程 SFT→训奖励模型(Bradley-Terry)→PPO 优化；加 KL 惩罚防止跑偏骗奖励。
>
> **得分点：**
>
> **(a)** 人类偏好是**相对排序**、难量化为绝对标签 → 监督回归缺乏可靠目标值。RL 视角：**state** = prompt + 已生成的 tokens；**action** = 下一个 token；**reward** = 对完整回复的人类偏好分（常仅末步给出）；policy = LM 的 next-token 分布。
>
> **(b)** **SFT**（示范数据监督微调，得初始策略与参考）→ **Reward Model**（用偏好对 $(y_w,y_l)$ 以 **Bradley-Terry** $P(y_w\succ y_l)=\sigma(r(x,y_w)-r(x,y_l))$ 训练标量奖励）→ **PPO**（用 RM 奖励 + advantage 优化 policy，clip 限制步长）。
>
> **(c)** 在目标里加 **KL 惩罚** $-\beta D_\text{KL}(\pi_\theta\|\pi_\text{ref})$，把策略**锚定在 SFT 参考附近**。因为 RM 只在参考分布附近可靠，远离后容易被钻空子（reward hacking）；KL 惩罚抑制策略漂移到 RM 没见过的奇异区域 → 保持生成质量、防止刷分。

---

> **复习联动**：选择题 + Q1–Q6 落点在四组核心笔记；Q5 coding 仿真题 Q7（GCN + InfoNCE）→ 速查表 Week6、Week11；Q7（Neural ODE）、Q8（RLHF）为自拟 novel-context 压轴。考点星级见 `考点梳理.md`。
